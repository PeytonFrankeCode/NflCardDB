__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/

# Local data + scraper output
data/*.db
data/*.db-wal
data/*.db-shm
data/html/
*.db
*.db-wal
*.db-shm

.pytest_cache/
.coverage
.DS_Store

# Browser profile the collector reuses between runs (cookies, cache)
data/browser-profile/

# Listing photos downloaded for OCR. Disposable -- deleting it costs a re-fetch.
data/photo-cache/
doctor-report.txt
install-log.txt

# Unattended run logs, one per day -- local diagnostics, not project files
logs/

# Working database -- rewritten by every run, not a project file
*.sqlite
*.sqlite-wal
*.sqlite-shm
api/import.sql
api/node_modules/
api/.wrangler/
api-details.txt

# Cloudflare token and account id, saved so the scripts stop asking
data/cloudflare.txt
