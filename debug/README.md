# debug

Drop-off point for pages that need looking at.

`capture.bat` writes here on purpose, rather than into `data/html/`, because
that folder is gitignored -- so GitHub Desktop never shows those files and they
cannot be sent anywhere. Anything in here is meant to be committed and shared.

Safe to empty whenever you like.
